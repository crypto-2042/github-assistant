{
  "name": "current-page-readme",
  "description": "README markdown text resource from the current GitHub repository page",
  "itemType": "resource",
  "pathPattern": "^/[^/]+/[^/]+/?$",
  "manifest": {
    "resourceType": "text/markdown",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "readme": { "type": "string" },
        "path": { "type": "string" }
      },
      "required": ["readme", "path"]
    },
    "page": "() => { const el = document.querySelector('#readme article.markdown-body, article.markdown-body, #readme [data-testid=\\\"markdown-body\\\"]'); const readme = (el?.innerText || '').trim(); return { readme, path: location.pathname }; }",
    "execute": "async ({ page }) => ({ readme: page?.readme || '', path: page?.path || location.pathname })"
  }
}
